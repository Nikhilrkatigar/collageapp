import 'package:flutter/material.dart';

class StudentListTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Student List - Admin'));
  }
}
